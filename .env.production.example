# Copy to .env.production ON THE SERVER. Never commit the real values.
DOMAIN=api.example.com
# Generate independently: openssl rand -hex 32
POSTGRES_PASSWORD=replace-with-random-hex-password
# Generate independently: openssl rand -hex 48
JWT_KEY=replace-with-random-signing-key
